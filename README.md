# prysm

**the visual protocol for [[superintelligence]].**

a small, formal, complete design system. every screen in [[cyb]] is generated by prysm. nothing about how it renders is accidental — the system is a pure function from element tree to coordinates, validated by 14 theorems in Lean 4

> **speed.energy.sex** — three principles. one move: subtract.

---

## what's groundbreaking

```
the first    — layout composition algebra published for UI
the only     — UI system with theorems proved in Lean 4 (no `sorry`)
the smallest — 13 atoms cover everything: text → cybergraph fields → 3D meshes
the cleanest — 3 substrate atoms for 3 geometric dimensions (2D · 1D · 0D)
the truest   — every value is visibly proven, asserted, or local
the fastest  — layout O(n), under 2ms for 10,000 organelles
the rarest   — emotion is computed from chain state, never painted by hand
the boldest  — black (#000000) is the energetically correct default
```

every other UI library renders pixels. prysm renders **meaning**

---

## the system

```
13 atoms       — 4 families, irreducible primitives
24 molecules   — composed widgets, transferable across cells
10 cells       — full-screen applications
6 foundations  — the specs that generate it all
14 theorems    — Lean 4 proofs, layout completeness
9 emotions     — color signals from the [[color-emotion spectrum]]
8pt quantum    — derived from visual acuity + Fitts's law
1 mantra       — don't trust, don't fear, don't beg
```

---

## atoms · 13 in 4 families

every visual artifact in cyb decomposes into these and only these

**substrate** — geometric presence by dimension

| atom | dim | role |
|------|-----|------|
| `glass` | 2D | translucent membrane — the surface every container is made of |
| `saber` | 1D | line with glow — demarcation, accent, [[cyberlink]] edge |
| `ion` | 0D | luminous point — charge emission, presence, energy |

**particle** — cybergraph content, each a distinct rendering pipeline

| atom | content |
|------|---------|
| `text` | language — prose, markdown, inline |
| `image` | raster — PNG, JPG, WebP, GIF (static or auto-animated) |
| `vector` | 2D paths — SVG particles, glyphs (emotion-driven), sigils (brand-fixed) |
| `mesh` | 3D geometry — glTF, scene graph, PBR, skeleton, animation |
| `media` | temporal — video, audio, streams with controllable playhead |
| `code` | formal notation — source code and math. math is code |
| `field` | mathematical field — scalar/vector function over a domain (e.g. [[cyberank]]) |

**identity** — name-resolved primitives

| atom | identifies |
|------|------------|
| `neuron` | actor — any agent in the graph (bech32) |
| `token` | asset — any tradeable value (symbol + chain) |

**input** — the one atom that accepts keystrokes

| atom | role |
|------|------|
| `entry` | form field — buffer, insertion point, selection |

---

## molecules · 24

composed widgets with intrinsic shape and behavior. transferable between cells. each declares its fold conformations and emotion bindings

`button` · `toggle` · `slider` · `tabs` · `mind` · `neuron-card` · `avatar` · `address` · `counter` · `content` · `display` · `launcher` · `adviser` · `input` · `filter` · `table` · `bar` · `pill` · `stars` · `widget` · `time-widget` · `graph` · `brain` · `menu`

---

## cells · 10

full-screen applications composing molecules for a domain. cells live in [[aos]]

[[cyb/oracle]] · [[cyb/brain]] · [[cyb/portal]] · [[cyberver]] · [[cyb/sense]] · [[cyb/sigma]] · [[teleport]] · [[sphere]] · [[warp]] · [[aos/hfr]]

---

## foundations · 6 specs, one move each

read in order

| spec | move |
|------|------|
| [[prysm/principles]] | **why** — speed · energy · sex |
| [[prysm/composition]] | **what** — the atom → molecule → cell model |
| [[prysm/layout]] | **space** — Π/Φ/K + quantum + fold + gravity (14 theorems, Lean 4) |
| [[prysm/emotion]] | **color** — the emotion function + the full palette |
| [[prysm/interaction]] | **response** — state machines per gesture |
| [[prysm/proof]] | **truth** — how [[zheng]] proofs surface in the interface |

---

## the stack

prysm is the visual layer of the [[soft3]] stack — the lens through which the [[cybergraph]] becomes perceivable

```
neurons · cells
    ↓
  prysm                      ← this repo
    ↓
soft3 SDK · cybergraph · bbg · zheng · radio · tru
```

every atom reads the stack. emotion flows from [[cyberank]]. identity from [[neuron]] keys. proofs from [[zheng]]. content from [[radio]]. rank from [[tru]]. prysm is not just rendering — it is the interface contract between a human and a proven knowledge graph

---

## source tree

```
system/
  specs/    principles · composition · layout · emotion · interaction · proof
  rs/       grid, scrollback, theme, particle
  proofs/   Lean 4 — algebra, container, fold, gravity, multimodal, protocol, sizing

atoms/specs/      13 atoms · 4 families (substrate · particle · identity · input)
molecules/specs/  24 molecules
chroma/specs/     grid + 9 zone specs (the concrete cyb screen composition)
rs/lib.rs         crate entry
```

---

## status

| | |
|---|---|
| atoms | 13 specified |
| molecules | 24 specified |
| cells | 10 named, [[aos]] in progress |
| foundations | 6 specs complete |
| Lean 4 proofs | 14 theorems, no `sorry` |
| emotion palette | 9 values, every source defined |
| proof layer | verified glyph + inspector + freshness |

---

*don't trust. don't fear. don't beg.*
